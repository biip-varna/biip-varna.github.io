(function(){ const isEn=window.location.pathname.includes('-en'); const file=isEn?'/includes/recent-articles-en.json':'/includes/recent-articles.json'; fetch(file).then(r=>r.json()).then(data=>{ const c=document.getElementById('recent-articles'); if(!c)return; c.innerHTML=`<h3>${isEn?'Recent Articles':'Последни статии'}</h3>`+data.map(a=>`<a href="${a.url}">${a.title} <small>(${a.date})</small></a>`).join(''); }); })();
